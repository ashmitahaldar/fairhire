# Database (Supabase connection string)
DATABASE_URL=

# Clerk — backend secret key
CLERK_SECRET_KEY=

# Clerk — frontend publishable key (VITE_ prefix exposes it to the browser)
VITE_CLERK_PUBLISHABLE_KEY=

# Shared secret between api and analysis engine for /internal/* routes
INTERNAL_API_SECRET=

# Frontend → API base URL (e.g. http://localhost:3001 in dev, Render URL in prod)
VITE_API_BASE_URL=

# LLM provider — not needed until Week 2
# ANTHROPIC_API_KEY=
# OPENAI_API_KEY=
