{
  "buildCommand": "npm run build --workspace=shared && npm run build --workspace=web",
  "outputDirectory": "web/dist",
  "installCommand": "npm install",
  "rewrites": [{ "source": "/(.*)", "destination": "/" }]
}
